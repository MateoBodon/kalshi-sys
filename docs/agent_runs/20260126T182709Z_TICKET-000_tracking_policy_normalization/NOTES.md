# Notes

- .git/info/exclude is at default comments only (no shared-policy rules).
- .gitignore already contains canonical scratch zones and agent scratch ignores; verified docs/agent_runs is not ignored.
- Untracked tickets and agent_runs directories present and should be tracked per TRACKING_POLICY.md.
- Found a 1.6G DIFF.patch in an older run log; moved to reports/_runs and left a placeholder.
