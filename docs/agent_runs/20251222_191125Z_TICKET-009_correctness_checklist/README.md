# Run README

Goal: Ticket #9 correctness checklist + merge to main.

Summary:
- Verified unit, runbook, and EC2 runtime evidence for venv-based systemd (paper-only).
- Confirmed scipy/pandas are required by index runtime imports; EC2 pip install succeeded without source builds.

Commands:
- See COMMANDS.md and commands.log.

Tests:
- See TESTS.md.
