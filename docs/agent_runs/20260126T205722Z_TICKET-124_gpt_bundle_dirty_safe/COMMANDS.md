# Commands

- `git status --porcelain` (exit 0)
- `ls -la tools/agentic 2>/dev/null || true` (exit 0)
- `ls -la scripts 2>/dev/null || true` (exit 0)
- `rg -n "gpt_bundle|bundle" -S . || true` (exit 0)
- `sed -n '1,220p' tools/agentic/gpt_bundle.py` (exit 0)
- `sed -n '1,200p' tools/agentic/project_state_refresh.py` (exit 0)
- `rg -n "artifacts/_local/bundles" -S docs tools` (exit 0)
- `mkdir -p artifacts/_local/gpt_bundles` (exit 0)
- `git checkout -b codex/TICKET-124_gpt_bundle_dirty_safe` (exit 0)
- `cat <<'EOF' > docs/tickets/TICKET-124.md ...` (exit 0)
- `git status --porcelain > /tmp/status_before.txt` (exit 0)
- `printf "dirty test\n" > tmp/_bundle_dirty_test.txt` (exit 0)
- `python3 tools/agentic/gpt_bundle.py --zip --ticket TICKET-DIRTY-TEST` (exit 0; dirty: yes, stash: yes)
- `git status --porcelain > /tmp/status_after.txt` (exit 0)
- `diff -u /tmp/status_before.txt /tmp/status_after.txt` (exit 0)
- `date -u +%Y%m%dT%H%M%SZ` (exit 0)
- `mkdir -p docs/agent_runs/20260126T205722Z_TICKET-124_gpt_bundle_dirty_safe` (exit 0)
- `git rev-parse HEAD` (exit 0)
- `git rev-parse --abbrev-ref HEAD` (exit 0)
- `. .venv/bin/activate && pytest -q` (exit 1; 2 failures in tests/test_index_scanner_fixtures.py)
- `git diff > docs/agent_runs/20260126T205722Z_TICKET-124_gpt_bundle_dirty_safe/DIFF.patch` (exit 0)
