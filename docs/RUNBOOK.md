# RUNBOOK

How to run, test, and debug this repo.

## Setup
- (commands)

## Build
- (commands)

## Test
- (commands)

## Routine artifacts
- Settlement basis audit (all series): `python tools/settlement_basis_audit.py --day <YYYY-MM-DD> --all-series`
- Archive basis artifacts into a run log: add `--runlog docs/agent_runs/<RUN_NAME>` (or `--archive-dir`)

## Common troubleshooting
- (bullets)
